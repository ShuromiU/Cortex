# Deferred Work

Items surfaced during review that are real but not actionable in the story that found them.

## Deferred from: story-5.1 (2026-08-06)

- **An eventless ended session never leaves the unconsolidated pool, and the pool is already large.** `getUnconsolidatedSessions` (`src/db/store.ts`) selects every `status='ended'` session with no `state` row at `layer='session'`; `consolidateLevel1` (`src/capture/consolidate.ts`) returns `[]` when the session has no `events`, so `writeSessionSummary` is never called and the row stays in the set permanently. The `inject-header` action (`src/transports/cli.ts`) walks that set on every SessionStart, and `src/db/gc.ts` never deletes `sessions` rows at all. *(Symbols, not line numbers: the citations in this file were measurably off by one within a day of being written, because every edit above them shifts the count.)*

  **Measured across all live stores, 2026-08-06:** **1,433 of 1,971 sessions are in the pool and 100% of them are eventless.** Worst store: 956. The absolute counts drift upward as sessions accumulate — an earlier reading the same day was 1,423 of 1,952 — so the durable finding is the *shape*: the pool is entirely eventless sessions that can never leave. **Children were 0 of the pool until this story's own live proof created the first one**, a subagent that captured nothing; the count is now 1 and will grow slowly. So the mechanism is pre-existing and general, affecting primary sessions — not something subagents introduce.

  **Deliberately not fixed in 5.1, on measurement rather than judgement:** the loop costs **10.0 ms median for 953 sessions on the largest real store** (0.011 ms per session, 7 runs, min 9.9 / max 11.1) against B-1's 150 ms, and a `sessions` row is **172 bytes** (dbstat: 954 rows / 160 KB) against B-8's 50 MB. At this repo's busiest observed dispatch rate — 56 subagents in four days, of which zero captured nothing — the added growth is under 1 MB/year. A correct fix is general, not child-specific, and would rewrite consolidation state for 1,428 existing rows on the SessionStart path: an unattended data change of exactly the shape the Epic 4 rollout warns about, and not something to do inside a story whose ACs never mention it.

  **What a fix would need to decide:** writing an empty session summary is the obvious move and is *wrong* as-is — `writeSessionSummary` also inserts a `session_summary` **episode**, which projects into `memory_items` (AD-4), so it would seed retrieval with empty items. A `state` row alone would work for the pool but enters `getRecentStates`/`getRecentStatesByScope`. So the real options are a GC rule for ended eventless sessions, or a query-level exclusion. **Owner: ShuromiU (product call — it is a data change across every existing store, not a code cleanup).** Bring it to whichever story next owns session lifecycle or GC, with these numbers.

- **`getUnconsolidatedSessionsByScope` is not parentage-filtered**, and `buildProvisionalHeader` (`src/query/state.ts`) loops it running `getEventsBySession` plus `getActiveNotes` per row. Harmless at today's counts and dominated by the item above, but it means child sessions enter a `cortex_state` code path that the scoped session listings deliberately exclude them from. Decide it with the pool question, not separately.

- **`cortex stats` reports `Focus: unfocused` after ten subagent dispatches.** `src/transports/cli.ts` derives the focus line from `store.getRecentSessions(10)` — **not** parentage-filtered — and takes the first non-null `focus`; children carry none. Reproduced by review. Pre-existing shape, but Story 5.1 made it far easier to reach: before, ten subagents each had to make a *captured tool call* to fill that window; now every dispatch does. The honest fix is to filter that query on parentage, which is a rendering decision — it belongs with whichever story owns child-timeline surfacing (currently 5.3, per the re-filing above), not with the story that created the sessions.

- **`SubagentStart` adopts whatever primary is active in the STORE, which need not be the one for the payload's `cwd`.** `getCurrentSession` is not scope-filtered and one store is shared by every worktree of a repository, so with two engaged windows on two worktrees a dispatch from A can be parented to B's primary and inherit B's `scope_key`; A's later spool flush then finds no child under A's scope and creates a second one. Reproduced. Not fixed here, for a specific reason: this is `ensureScopedSession`'s deliberate Epic 0 contract — adopt the active primary *as-is*, because resolving scope from a subagent's `cwd` is exactly what ended and rotated the parent mid-turn before Story 0.1's review caught it — and the cheap alternative (calling `detectGitScope` in the hook) is a git subprocess on a path whose structural clause forbids one. Same family as the "two concurrent windows share `.cortex.state`" item from Story 4.5; a real fix is per-window session identity at the scope layer.

- **A re-fire for a recycled `agent_id` after the session tree ended lands on the ended child.** `getSessionByAgentId` filters by neither parent nor status — deliberately, because Story 0.2 AC #3 requires a child to stay findable after its parent ends — so a dispatch reusing an id resolves to the old, ended row and files new activity under it. Reproduced. Bounded by the host reporting per-dispatch UUIDs, which is already logged above as "agent-id uniqueness across primaries is a load-bearing host assumption"; Story 5.1 makes it reachable at dispatch rather than only at flush. Fixing it means re-parenting or reactivating on reuse, which contradicts the AC that keeps the row findable — so it needs the same owner as that assumption, not a patch here.

## Deferred from: story-2.5 create-story (2026-07-31)

- **Two source files contain raw NUL bytes, and every text search silently skips them.** `src/transports/hook-entry.ts` (offset 11476) and `src/query/reference-validation.ts` (offsets 7606, 8053) each hold a literal `0x00` inside a template literal used as a cache-key separator — `` `${a}\0${b}` `` written as an actual control byte rather than the escape. The strings are behaviourally correct, so nothing fails; the cost is entirely in tooling. ripgrep, grep, and `certify_refs`'s text pass all classify these files as binary and omit them **without saying so** — `rg -n "findDbPath" src/transports/` returns two files, `rg -na` returns three. Story 2.5 hit this directly: `hook-entry.ts` holds one of the four `findDbPath` derivations, and a text-only enumeration would have relocated the store while leaving the SessionStart transport pointing at the old path. `certify_refs` on `.cortex.db` returned 17 union sites and did not include it either.

  Provenance: `reference-validation.ts` from `4f95366`/`43bf0d9`, `hook-entry.ts` from Epic 0's `1982226` — the bash-heredoc escape-collapse failure mode, where `\0` in a heredoc becomes the byte it denotes. **Fix is three characters per site** (`\0` written as source text), changes no behaviour, and is worth doing standalone rather than inside a story that would then have to prove the source edit and the relocation are independent. Until then, enumerate with `rg -a` or via LSP in these two files, and treat "text search found nothing here" as unproven.

## Deferred from: story-2.5 implementation (2026-08-01)

- **A long-running MCP server keeps writing to the pre-relocation store.** It resolves its database path once at startup, so a server already running when the package is upgraded holds the old `<project>/.cortex.db` until it is restarted. Observed live: the original kept accruing sessions for hours after migration while the new store accrued its own. Nothing is lost — the original is retained by AC #3 anyway — but the two diverge, and whichever session restarts last "wins" the newer memory. A fix means either detecting the moved store from inside a running process or having `install`/`doctor` tell the user to restart. Belongs with whatever story next touches MCP lifecycle.

- ~~**Two worktrees that each carried their own pre-relocation `.cortex.db` migrate only one of them.**~~ **PARTLY CLOSED in the 2.5 review repair.** The original entry was wrong on its own mitigation: it claimed `doctor` would report the un-migrated file as "not migrated yet" forever, and `doctor` in fact reported **"migrated"** and told the user to delete it — the inverted safety net a reviewer caught. Two fixes landed: the search order now puts the **main** worktree first (a first command run inside a linked worktree was migrating that worktree's small store and permanently shadowing the main checkout's real one — measured at 60 notes replaced by 0), and `doctor` now reports migration from the recorded `migrated_from` rather than inferring it. What remains deferred is genuinely only the merge: when two worktrees *both* hold a pre-relocation store, the main one wins and the other is reported as present-and-not-migrated. Merging is a real data question (duplicate ids, conflicting notes, overlapping sessions) and needs a `cortex import <db>` with a documented conflict rule.

- **A sibling checkout that is transiently unavailable can be adopted away.** `findAdoptionCandidates` matches on the root-commit OID, which clones and forks share, and treats "recorded path does not exist" as proof the store is orphaned. An unmounted share, a folder mid-rename or a restore in progress satisfies that. Adoption is a move, so the live checkout loses its store and is not even offered it back. Mitigated for now by preview-by-default plus an explicit warning in both the `adopt` preview and `doctor`'s fix text; a real fix needs a liveness signal stronger than `existsSync` — a recorded machine/volume id, or a lock file the owning checkout refreshes.

- **`findAdoptionCandidates` opens every store under `$CORTEX_HOME/projects`.** Fine at today's scale (one file open and two `meta` reads each, only when the current project has no store at all), but it is O(projects) on a cold start for a moved repository. If a user accumulates hundreds of projects this wants an index — a small `projects.json` keyed by root-commit OID — rather than a directory scan.

- **Adoption clears only the first candidate.** `cortex adopt` acts on `candidates[0]`, sorted by size, and there is no way to choose another or to see more than the largest in `doctor`'s single line. Two repositories sharing a root commit (a fork of a fork) whose paths are both gone would present two, and the CLI offers no selection. Rare enough to defer; needs `cortex adopt --from <path>`.

## Deferred from: code review of story-0.1 (2026-07-24)

- ~~**Command-failure episodes fold across the parent/child boundary.**~~ **CLOSED in story 0.2.** The fold is now scoped to the recording session. The original reasoning below was incomplete: the deferral assumed the fold was harmless because it matched pre-0.1 behavior, which held only when the *primary* failed first. Story 0.2's review reproduced the child-first ordering — the parent's later identical failure folded into a **child-owned** episode and the parent got none, which pre-0.1 it would have. Resolution: `findRecentEpisodeBySummary` takes the recording session and matches only its own episodes. Two sessions that each hit one failure each keep an episode; a retry loop within one session still folds. Both orderings are pinned by test; all five locked eval suites at zero delta. Original detail: `findRecentEpisodeBySummary` (`src/db/store.ts`) filters by neither session nor parentage, so a subagent's failing command within 24h of an identical parent failure bumps the *parent's* episode row and re-upserts the parent-owned `memory_items` projection at `(seen 2x)`; the child gets no episode of its own. This leaks story 0.1's AC #2 ("attributed to the child, never the parent"). **Deferred to Story 0.2** — unreachable in the shipped wiring because PostToolUse never spawns Node, and it goes live exactly when 0.2 wires the spool. Decide it there, with the real capture path in hand. Note that any fix changes cross-session dedup for *all* sessions, not just children, and that path feeds `memory_items` — re-run the locked eval suites.

- ~~**`reflect-pre` bills subagent edits to the primary session.**~~ **CLOSED in Story 5.2 (2026-08-06), both halves together as the note asked.** `reflectFromPayload` now resolves with `agentIdentity(payload)`, so a subagent's reflex is billed to the child and writes its own dedupe file — the parent keeps its once-per-anchor marker. The cost the note feared did not materialise: `reflectMemory` uses the session id **only** for the state-file path and the ledger row, so nothing rendered depends on it and the eval gate was unmoved at 9/9. The coupled half is closed in the same change: `bookHookInjection` takes an optional session id, the subagent brief passes the child's, and both pre-existing callers (`renderConsultGate`, `endOfTurn`) keep booking to the primary because they are parent-facing. Three tests pin it, including one that fails if the identity argument is removed. Original detail: `reflectFromPayload` (`src/transports/hook-entry.ts`) resolved without agent identity, while the PreToolUse hook fires on `Edit|Write` for subagent tool calls too. The reflex ledger entry is charged to the primary, and because the reflex dedupe state file is keyed by session id, a subagent consumes the once-per-anchor marker — the parent then edits the same file and gets no `additionalContext`. Pre-existing and unchanged by story 0.1, which deliberately kept the `reflect-*` and `end-of-turn` actions on the primary. ~~Epic 5 owns parent/child reflex routing.~~ **RE-FILED to Story 5.2 (2026-08-06).** Story 5.1 deliberately did not touch it: it wires a new event and threads identity through that event only, and re-routing `reflect-*` would move a rendered surface on every Edit and Write. 5.2 is the right owner because it is the story that decides what a subagent is told and who pays for it — and it inherits the same defect one layer over: `bookHookInjection` books against `getCurrentSession()`, which is primary-only, so a brief injected into a subagent would be billed to the parent. Both are the same question and should be answered once.

- **Subagent file activity is captured but surfaced nowhere.** Raw `read`/`edit`/`write` events never project into `memory_items` (only command runs, episodes, notes and snapshots do), and story 0.1's Task 6 made `getRecentSessionsByScope`, `getSessionCountByScope` and `syncBranchSnapshotForSession` primary-only. A subagent that reads and edits a file twelve times therefore contributes nothing to the recent-files line, the branch snapshot, or the session summary. The filtering is correct — the branch snapshot is the primary timeline's summary — but no alternative read path exists. ~~Epic 5 owns child-timeline rendering (Story 5.1 AC 2).~~ **RE-FILED to Story 5.3 (2026-08-06).** Story 5.1's AC 2 is narrower than this note assumed: it requires only that a child's activity is *attributed to the child rather than merged into the parent*, which Epic 0 already delivered and 5.1 pinned by test. Rendering a child's work into a surface a human or agent reads is a different job, and it belongs with the story that decides what a subagent's work is worth keeping — 5.3, which owns write-back. Note the enumeration this needs: `getRecentSessions`, `getSessionCount`, `getUnconsolidatedSessions`, `getUnconsolidatedSessionsByScope`, `scopeRootFor` and `scopeKeysForRoot` are **not** parentage-filtered, so children are already visible there; the filtered set is `getCurrentSession`, `getRecentPrimarySessions`, `getRecentSessionsByScope`, `getSessionCountByScope` and `syncBranchSnapshotForSession` (enumerated with `find_referencing_symbols`).

- ~~**README carries no session-identity model.**~~ **CLOSED in story 0.2** — README gained a "Subagent attribution" section covering `(scope_key, agent_id)`, per-line spool identity, primary-only scoped reads, and the stale-hook symptom with its fix.

## Deferred from: code review of story-1.1 (2026-07-27)

Three review layers produced 24 findings; 17 were fixed in the story. These are the rest — each real, none able to produce a wrong contradiction verdict.

- **Contradiction detection is ASCII-only.** `TOKEN_PATTERN` (`src/memory/text.ts`) is `[a-z0-9][a-z0-9._/-]*`, so Cyrillic, CJK, fullwidth and accented content analyzes to an empty core and never conflicts. Fails safe — a silent miss, not a wrong answer — and is now stated as a known limit in `conflict.ts` and the README. A `\p{L}\p{N}` pattern with the `u` flag would close it, but `TOKEN_PATTERN` is shared with `query/retrieval.ts` and the rerank layer, so changing it moves `output_tokens` on the locked suites and needs its own story with a gate run.

- **RE-FILED to Story 5.3 (2026-08-06):** the item below assigns `promoteSubagentNotes`' re-activation defect to "Epic 5" without naming a story. It belongs to 5.3, which owns subagent write-back and is the only story that would give this function a caller — it still has **zero runtime call sites**: `find_referencing_symbols` returns only the `src/index.ts` barrel export, and text search adds only `tests/consolidate.test.ts`, which exercises it directly. So it remains dormant capability rather than live code. Story 5.1 does not call it.

- **`promoteSubagentNotes` re-activates an arbitrary prior.** `src/capture/consolidate.ts:288-310` picks the *first* same-kind+subject parent note via `Array.prototype.find` — including one superseded long before the child ran — and re-activates it unconditionally. That was deterministic when only one note per (kind, subject) could be active; the AD-17 veto ends that guarantee, so a promotion can now leave three active decisions on one subject. Story 1.1 narrowed the blast radius (the already-contested veto stops the extras being silently archived) but did not fix the re-activation itself. Not reachable from the live hook path — `promoteSubagentNotes` has no in-repo runtime caller, only `src/index.ts`'s export and `tests/consolidate.test.ts` — so it is reachable through the library surface only. Epic 5 owns subagent note promotion and should fix it there.

- **`cortex_resolve --subject` in the CLI has the same ambiguity the MCP tool now refuses.** `src/transports/cli.ts:497` still calls `findActiveNoteBySubject` directly. The MCP path refuses to guess while a contest is open; the CLI path does not. Same fix applies, but the CLI resolve command has no test coverage for contested subjects and adding it is a small story of its own.

- **`notes.conflict` carries no counterpart id.** Conflicts are cleared per *subject*, so resolving one side clears every contested note on that subject. With three-way contests this over-clears — a pair unrelated to the resolved one loses its marker. Correct for the two-note case, which is what detection produces today. Story 2.2's deletion path inherits exactly this limitation, deliberately. A `note_conflicts(note_id, counterpart_id)` join table would make it exact and would also give SM-5 a per-pair resolution rate, but it is a new table. ~~R1 has already spent its one `SCHEMA_VERSION` bump on Story 3.1.~~ **Corrected in Story 2.2:** that was written before the epic ordering settled. Epic 2 runs before Epic 3, so **Story 2.2** made the single 4 → 5 bump and created `V5_TABLES`; every later story appends to that constant. A join table would therefore be an *append*, not a second bump — the objection is weaker than recorded, and the item stands on cost alone.

## Deferred from: code review of story-2.2 (2026-07-28)

- **The IMMEDIATE-transaction guarantee has no automated test.** `updateMemoryItemText` and `deleteMemoryItemCascade` now use `runInImmediateTransaction`, matching `insertNote`/`updateNoteStatus` and the rule CLAUDE.md states. The failure it prevents — `SQLITE_BUSY_SNAPSHOT` bypassing the busy handler on a lazy lock upgrade — is only observable when a **second process** holds the write lock; a reviewer reproduced it that way. An in-process attempt passed under both DEFERRED and IMMEDIATE, so it was removed rather than kept as a test that proves nothing. Closing this needs a two-process harness (spawn a child that opens the same file and holds a write transaction), which is a small story of its own and would also cover `insertNote`'s identical guarantee, currently equally untested.
- **`cortex gc`'s command-run overflow prune is undone by the backfill.** `gc.ts` deletes overflow `command_run` rows from `memory_items` without touching `command_runs` or the `events` rows behind them, so the next `ensureCortexSchema` rebuilds every one — measured by a reviewer at 5 items → gc → 2 → next command → 5. The cap bounds nothing. Story 2.2 documented the resurrection mechanism and fixed it for the deletion path; gc has the same bug and was explicitly out of scope. It should reuse `deleteMemoryItemCascade`'s chain rather than deleting projections directly.
- **`edit-memory` never re-runs or clears contradiction detection.** Editing a contested decision so that it no longer contradicts its counterpart leaves both notes at `conflict = 1` and both projections carrying `Conflict: true`, so they render `[contested]` forever; editing a note *into* a contradiction flags nothing. The correction command therefore cannot correct a false contest — only `cortex_resolve` can, and only by resolving a note the user wants to keep active. Re-running detection on edit is the obvious fix and is a behavior change worth its own story, since it can newly mark items contested as a side effect of a typo fix.
- **`note-resolve --status` silently coerces anything that is not `superseded` to `resolved`.** A typo like `--status supersede` resolves instead, which now also clears the contest and skips the FR-4 demotion. Pre-existing line; the consequences grew with this story. Validate against the closed set.
- **`--subject` normalisation differs between the guard and the resolver.** `getActiveNotesBySubject` normalises with `.trim().toLowerCase()`; `findActiveNoteBySubject` passes the raw string with `COLLATE NOCASE`. `--subject " auth "` therefore reaches a guard that sees the notes and a resolver that does not, and `COLLATE NOCASE` is ASCII-only while `toLowerCase()` is Unicode-aware. Same family as the missing tiebreaker below.

## Deferred from: code review of story-2.4 (2026-07-31)

- **`install` repairs the wiring in the file it owns, not in the others.** It now reads all three settings files to avoid duplicating an event another one wires, but if that other file's entry is itself wrong — a stale matcher in `settings.local.json`, say — the run reports `unchanged` and the defect stands. Repairing across files means writing to a file the user did not name in `--scope`, which is a bigger decision than this story should take.
- **A `.bak` per script and per settings file, with no rotation.** Every overwrite of existing content now writes one, so a user who runs `install` repeatedly through several Node upgrades keeps only the most recent previous copy. Adequate for the "did it just eat my edit" question, useless as history. Belongs with a general retention policy, alongside `memory_corrections`.
- **`--scope project` cannot register MCP when the project settings file is unparseable.** The refusal short-circuits the whole settings block, which contains the separate-file `.mcp.json` branch, so an unrelated syntax error silently cancels a write to a different file and no `mcp` action is reported at all.
- **Shell-metacharacter refusal is a whitelist decision made once, at the hooks directory.** A path containing `$`, a backtick or a backslash is refused rather than escaped, because escaping correctly requires knowing whether the host runs the command through `bash` or `cmd.exe`. If a user genuinely has such a home directory, `--dir` is the only route and they must wire it themselves.
- **`renderInstallResult` and `runInstall` disagree about ordering guarantees.** Actions are applied in sequence and a refusal stops only its own action, so a refusal partway through leaves earlier writes in place. The summary now says so and lists them, but there is no rollback and no transaction. Making install atomic across four different file formats is a much larger design.

## Deferred from: story-2.4 implementation (2026-07-31)

- **`writeFileAtomic`'s guarantee has no automated test, for the same reason `insertNote`'s IMMEDIATE transaction does not.** The temp-file-plus-rename is unobservable from a single process: replacing it with a direct `writeFileSync` leaves every assertion green, including "no temp file left behind". Proving it needs crash injection or a concurrent reader observing a partial file — the same harness the Story 2.2 review already logged for `SQLITE_BUSY_SNAPSHOT`. Close both together; the mutation is reported as a survivor rather than counted as a kill.
- **`install` does not create the store or engage Cortex.** AC #1 enumerates four actions and neither is among them, so a fresh project fails the diagnostic on `engagement` and `database` and `cortex install` exits 1 on its first run. The command says so explicitly rather than leaving a bare red report, but "install then immediately run another command" is still a seam in the story whose purpose is that the install cliff is not the adoption cliff. Whether install should engage is a product call — it would mean creating a database as a side effect of installation.
- **The `install-hooks` alias silently widens what that name does.** It used to write three scripts and print JSON; it now also writes `~/.claude/settings.json` and the project's `.gitignore`. Non-destructive (merge, backup, refuse-on-unparseable, `--dry-run`), but a user with muscle memory gets more than they asked for. Considered keeping a narrow `install-hooks`; rejected because doing the merge is the whole improvement, and because two commands that install differently is the drift this story exists to remove.
- **Settings formatting is not preserved.** The file is parsed and re-serialised at two-space indent, so a user's own formatting and any comments are lost. Mitigated by the `.bak` and stated in the README. Preserving formatting needs an edit-in-place JSON writer, which is a dependency this repository does not have and would not add lightly.
- **Codex is still not installed or diagnosed.** `--codex` is accepted and only redirects the hooks directory; no Codex settings are written and `doctor` does not read them. Inherited from 2.3 and unchanged here.

## Deferred from: code review of story-2.3 (2026-07-31)

- **`doctor` still reads only two things out of an installed hook script: its stamp and its Node invocation.** A script whose body was hand-edited keeps a current stamp and is reported healthy. The currency check answers "does this predate the shipped template", not "has this been modified" — different questions, and AC #2 asks the first. Story 2.4's AC #3 ("a hook script the user has modified … refuses without explicit confirmation") needs the second and will have to compare content, not the stamp. Stated as a limit in the README rather than left implicit.
- **A three-way disagreement between settings files is invisible.** Hooks-directory discovery takes the first wired command; presence is now checked at every wired path, so a script missing anywhere fails — but the *reported* `hooks_dir` still names one directory, and a user with a project-level override plus a user-level default sees only one of them. Fixing it means per-directory reporting, which changes the report shape.
- **The interpreter is resolved, never executed, and so is `jq`.** A present-but-broken binary passes. Deliberate (it keeps B-7 structural and the negative cases testable) and stated in the README, but it does mean "jq availability: pass" is weaker than it reads.
- **`schema_version` is compared for equality, and the store is not integrity-checked.** `PRAGMA quick_check` is O(database) and would put the 3-second budget at risk on a large store, so a corrupt-but-openable database passes the reachability check. Only a store SQLite rejects outright is caught.

## Deferred from: story-2.3 implementation (2026-07-31)

- **`cortex doctor` knows nothing about Codex.** `install-hooks --codex` writes the same three scripts into `~/.codex/hooks`, but the diagnostic reads only Claude Code's settings files and MCP locations. A Codex install is reported as entirely unwired. Story 2.3's ACs name MCP registration and hook wiring without naming a harness, and every other AC clause (`jq`, Git Bash interpreter, N-6) is written from the Claude path; extending it needs the Codex settings schema pinned down first, which is its own piece of work.
- **`install-hooks` bakes paths without checking they exist.** It substitutes `process.execPath` and two `path.resolve`d entry points into each script and writes them unverified. Running it from a source checkout under vitest bakes `src/transports/cli.js`, which does not exist — caught only because `doctor` now reports it. The installer could verify its own substitutions; that is Story 2.4's territory, which owns install correctness.
- **Hooks-directory discovery takes the first wired script command it finds.** If two settings files wire cortex scripts from different directories — a project-level override plus the user-level default — only the first is diagnosed and the second is invisible. Correct for every installation `install-hooks` produces, since it writes one directory and prints one snippet. Diagnosing all of them means reporting per-directory results, which changes the report shape.
- **The stamp does not survive a user's own edit.** A user who hand-edits an installed hook keeps its stamp, so `doctor` reports it current. The check answers "does this predate the shipped template", not "has this been modified" — those are different questions and AC #2 asks the first. Story 2.4's AC #3 ("a hook script the user has modified … refuses without explicit confirmation") needs the second, and will need to compare content, not the stamp.

## Deferred from: story-2.2 implementation (2026-07-28)

- **`findActiveNoteBySubject` has no tiebreaker.** `src/db/store.ts` orders by `timestamp DESC LIMIT 1` with nothing after it, so two active notes on one subject written in the same millisecond resolve in an unspecified order — whichever SQLite happens to return. Reproduced while testing `note-resolve --subject` on the ordinary decision-plus-blocker case: which of the two closes is arbitrary. This is the same defect class as the `rowid DESC` tiebreaker Story 2.1 added to its paging query, and the fix is the same one word. It is **not** the ambiguity retrospective action item 2 named — that one was about *contested* pairs, and it is now refused on both the MCP and CLI paths. Left out of 2.2 because the story scoped "port the guard, do not redesign it", and changing which note resolves is a behavior change for both `cortex_resolve` and `note-resolve` that deserves its own test sweep. The 2.2 test asserts exactly one note resolved rather than pinning which, so it does not freeze the current arbitrary order.
- **`memory_corrections` has no retention rule.** Every edit and delete appends a row carrying the full prior text, and nothing prunes it — `cortex gc` does not know about the table. Growth is proportional to corrections, which are rare, so this is not urgent; but "deleted text stays readable until gc prunes it" is currently a promise gc cannot keep. Belongs with Story 4.6's eviction work, which owns retention policy for derived data.

## Deferred from: code review of story-1.5 (2026-07-25)

Low-severity items from two review rounds on the eval gate. None can make the gate report green while quality regresses; they are ergonomics, coverage and doc-accuracy gaps.

- **`scripts/check-baseline-justification.mjs` has no test coverage.** Only the pure `checkBaselineJustification(commits)` is unit-tested. The base resolution, `--name-status` parsing and range construction were verified by hand against a scratch git repo, twice, but nothing pins them. A test that builds a temp repo and runs the script would close it — Task 4 said "keep the check in a small script so it is testable", and it is testable, just untested.
- **`regenerateBaseline` writes unconditionally.** It prints the regressions it is about to bake in, but a trailer justifying an accepted token cost silently accepts any accuracy regression alongside it. Nothing correlates the stated reason with the accepted deltas. Considered requiring the trailer to name the metric; rejected as over-fitting for now.
- **The `concurrency` group does not de-duplicate push and pull_request runs.** `github.head_ref || github.ref` yields different keys for the two events, so a same-repo PR still runs the matrix twice. Same-event supersession does work. The comment in the workflow overstates it.
- **Stray non-`.json` files in `eval/baselines/` are ignored** while near-misses in `eval/suites/` fail. `listJsonNames` computes `unrecognized` for both and only consumes one. Asymmetric, harmless today.
- **A `null` entry inside `seed.items` throws** from `checkKindCoverage` outside the per-suite try/catch, suppressing the whole report. Fails closed but unreadably.
- **`--regenerate-baseline ../escape` rejects the write via an unhandled exception**, so it prints a Node stack trace rather than the clean message the empty-name path produces.
- **`CLAUDE.md` § Verification still tells humans `npx vitest run`** while CI was moved to `npm test` on the grounds that the declared script is authoritative.

## Deferred from: code review of story-0.2 (2026-07-24)

- **Concurrent spool appends tear lines above the stdio buffer.** `hooks/claude/cortex-capture.sh` appends with `printf '%s\n' "$LINE" >> "$SPOOL"`, which is atomic only up to roughly one stdio buffer. Measured by a reviewer with 16 concurrent hook invocations: 848-byte lines 16/16 intact, 2.8 KB 13/16, 53 KB 14/16 — torn lines are then silently dropped by `parseSpoolLines`. The `Bash` branch embeds uncapped `stdout`/`stderr`, so a real failing-test capture lands well past the safe size. Predates agent identity; parallel subagents make it easier to hit. Likely fix: cap the captured streams inside the existing jq program (e.g. `.[0:2000]`), which costs no extra process, and/or write via a per-PID temp file. Wants a measurement-backed story of its own.

- **B-4 (≤15 ms PostToolUse) is not achievable on the Windows/Git Bash target.** Measured ~400 ms per hook invocation on the dev machine; `bash -c 'exit 0'` alone is ~36 ms, so the budget is exceeded before the script runs a line. Pre-existing and not caused by any R1 story, but story 0.2's AC #1 binds it, so AC #1's third clause is **not met, with cause**. N-4 (no Node spawn per tool call) does hold. The budget needs re-basing against the real platform the way B-4a was already amended for the substitution path — a PRD decision, not a code fix. Carry to the Epic 0 retrospective.

- **Agent-id uniqueness across primaries is now a load-bearing host assumption.** `getSessionByAgentId` filters by neither parent nor status — deliberately, because story 0.2's AC #3 requires a child to stay findable after its parent ends. Consequence: if a host reuses an `agent_id` in one scope across two primaries, the second primary's subagent work is written into the first primary's (ended) child. Claude Code reports per-dispatch UUIDs so this does not arise today. Worth stating in the architecture spine rather than patching, since the alternative breaks AC #3.

## Deferred from: code review of story-1.2 (2026-07-27)

- **`cortex note-resolve` in the CLI leaves a resolved note rendering as contested.** `src/transports/cli.ts:504` calls only `updateNoteStatus`, never `clearConflictsForSubject`, so the projection keeps `Conflict: true` alongside `Status: resolved` and renders `… [contested] (resolved)` — while the surviving side keeps a bare `[contested]` forever. The MCP path (`src/transports/mcp.ts:515-524`) does clear. Story 1.2 made this visible on recall/brief/reflex where it was previously confined to `state`. Fixes together with the `--subject` ambiguity already logged from story 1.1.

- **The `status: resolved` sniff is still duplicated.** `src/query/render.ts` and `src/query/state.ts` each inline `item.text.toLowerCase().includes('status: resolved')`, one line from the now-centralized `isContested`. It carries the identical text-sniff flaw and the two copies can drift (they already differ in position relative to `renderReferenceLabel`). Centralize it when `notes.conflict` gets a real `memory_items` column — the release that can afford a `SCHEMA_VERSION` bump fixes both sniffs at once.

- **`recall` can exceed its token budget by the trimmed-hint line.** `assembleBudgeted` (`src/query/recall.ts:60-79`) refuses to drop the last evidence line so the budget can never silence the top result, then appends the hint regardless — 51 tokens against a requested 10. Pre-existing and not contested-specific; now pinned by a test in `tests/recall.test.ts` so it is at least visible. Deciding whether the hint should be droppable is a product call, not a bug fix.

- **FR-8's credit side has no production producer.** Story 3.5 built the accounting — evidence columns, the refusal of unevidenced credit, the AD-15 spool path booked exactly-once, and `unrealized` for observed declines — but the mechanism that *produces* a `saved` row is verified read substitution (Story 4.5), which is blocked on the B-4 / B-4a platform amendment. Consequence, stated so a reader does not mistake it for a defect: `cortex stats` shows `Saved: 0` and a negative `Net` on every real store, and will until 4.5 lands. The `tool: 'credit'` spool record has tests and no emitter in `src/` or `hooks/`.
- **`cortex stats` figures are cumulative, not per-session.** FR-9 (Story 3.6) is what adds the session/scope split, item counts by state, never-retrieved counts and the ten most-retrieved items.
- ~~**The eval gate covers only the recall surface.**~~ **Closed by Story 3.4.** `SurfaceFixture` now asserts `expect_contains`/`expect_excludes`/`max_tokens` over `brief`, `header` and `full_state`; `findSurfaceFailures` fails the gate on any of them, and `evaluateStore` builds the session brief (which it previously did not compute at all). Deleting the contested marker now turns the gate red, proven by mutation. `brief-surface` asserts on all three surfaces, and a `SurfaceFixture` may supply its own `budget` so the brief's token-budget enforcement is exercised at a budget that actually binds (44, where a 77-token brief must trim) rather than by a `max_tokens: 150` with 73 tokens of headroom. **Residual, deliberately not claimed as covered:** the brief's read-ledger line is excluded from the harness (`includeReadLedger: false`), because a seeded store cannot stage on-disk files with matching hashes and coupling the gate to the working tree would be worse.

- **The contested fixture's adversarial property is itself ungated.** `eval/suites/contested.json` exercises the reorder only because the uncontested insight (47.10) sits between the two contested decisions (53.00 / 45.30). If ranking ever drifts so the pair becomes naturally adjacent, the `"[contested]\nDecision ["` needle passes without the reorder running and nothing goes red. The unit tests pre-assert the split explicitly; the fixture format has no equivalent hook.

- **`state`'s kind-section grouping has no adversarial integration test.** Section order is hotness-based and no configuration reachable through the public API split a same-kind contested pair there, so the grouping call is defensive. It is covered by unit tests on `groupContestedAdjacent` / `groupContestedWithinKind`; an integration test that passes regardless was removed rather than kept.

- **`RecallOptions.limit` is uncapped against an O(n²) grouping scan.** `recall` is public API and `limit` passes straight through; `groupContestedAdjacent` is quadratic in the result count. In-repo callers use n = 8 (`src/transports/mcp.ts` never sets it), so this is theoretical today, but `recall(store, topic, { limit: 5000 })` with all results contested would run ~12.5M synchronous iterations.

## Deferred from: code review of story-1.3 (2026-07-27)

- **Pass 1 of `assembleBudgeted` is a greedy prefix that can leave budget unspent.** `src/query/recall.ts` stops at the first evidence line too long to fit and never reconsiders, so a short lower-ranked result is dropped while budget remains. Measured by a reviewer: three entries at budget 60 rendered one line plus `…2 more matches trimmed` with 35 tokens unused and a 10-token decision trimmed. Pre-existing and untouched by story 1.3, but the story made it *visible* — an alternatives line rendering next to a trimmed decision reads as a trade that did not happen. Fixing it means a best-fit or continue-past-a-miss pass, which changes drop order for every surface using the function; that is a product call about whether rank order or budget utilization wins.

- **Continuations are not charged for their newline separator.** Pass 2 charges `estimateTokens(continuation)` but the assembled output joins with `\n`, so a rendered result can exceed its budget by `ceil((lines-1)/4)` — about 4 tokens on a 160-token budget with 8 results all carrying alternatives. The same gap already existed for primary lines; story 1.3 roughly doubles the exposure by doubling the line count. A reviewer swept real `recall` budgets 1–700 and found it never triggers in practice: every overshoot observed there is the pre-existing always-keep-the-first-line + trimmed-hint behavior already logged from story 1.2. Fix alongside that one, since both are the same accounting question.

## Deferred from: code review of story-1.4 (2026-07-27)

- **Superseded items are never collected — growth is now unbounded on the automatic path.** Demotion floors at `cold` and GC deletes only `state = 'archived' AND access_count = 0` (`src/db/gc.ts:113`), so nothing ever collects a superseded item. CLAUDE.md frames this as parity with `resolved`, but resolve is a rare manual close-out while supersede fires on every same-subject decision/intent revision — each iteration of a decision now permanently adds a retrievable FTS row. Needs a lifecycle rule (aged-cold-superseded → archived after N days without access, or a GC category) — a product call that belongs with FR-16/Story 4.6's eviction work, not a rendering story.

- **"Ranked below the current one" is a tendency, not a guarantee.** A superseded predecessor with heavy access asymmetry can outrank its active successor at retrieval (reproduced by a reviewer: both aged 30+ days, predecessor touched 10× → 41.5 vs 39.0, retired decision at rank 0 with the `Most relevant —` header pointing at it). The label is the real guarantee and docs now say so. If the inversion matters in practice, the principled fix is a display-order rule in `recall.ts` (active-before-superseded within a `(scope_key, subject)` pair — the `groupContestedAdjacent` precedent, gate-safe because it never touches `retrieveMemory`) rather than an unwinnable retrieval-penalty arms race.

- **The `touchMemoryItems` SQL CASE still reads status by substring LIKE** while the JS predicates are now trailer-scoped and kind-guarded — a spoofed content substring makes touch preserve state where derive treats the item as active. Self-healing (the next refresh re-settles) and exactly the divergence the resolved branch has carried all along; fold into the existing "resolved sniff duplication" item — the release that adds real status columns to `memory_items` fixes all of it at once.

## Deferred from: story-2.7 (2026-08-01)

- ~~**A second source file carries a raw NUL byte, and the standing warning names only one.**~~ **CLOSED by story-4.5 (2026-08-03).** The set was actually **three**, not two — `src/capture/spool.ts` also carried one (`creditRowId`'s join separator), found by that story's mandatory byte-scan and missed by this note's enumeration. All four occurrences across the three files were the same shape (a composite-key separator) and are now the six-character escape, identical at runtime. `CLAUDE.md`'s three mentions are corrected, and a test walks `src/`, `hooks/` and `tests/` failing on any control byte but tab, LF and CR — so the enumeration is no longer hand-maintained. Original note follows. `CLAUDE.md`'s Serena/RefCertify bullet cites `src/transports/hook-entry.ts` as the file text search silently skips. Enumerated during this story (`comm` of `grep -rlI` against the full `*.ts` list): the set is **two** files — `src/transports/hook-entry.ts` *and* `src/query/reference-validation.ts`. Hit live here, in this story: `grep -n "^export" src/query/reference-validation.ts` returned `Binary file … matches` and nothing else, while `grep -a` returned the content. That file is the freshness leg's implementation — `buildValidationLabel`, the `[stale:`/`[moved:` producer — so it is squarely on the path of the retrieval work Epics 3 and 4 will do. Not fixed here for two reasons: the bullet lives inside `CLAUDE.md`'s "Agent Tooling" section, which carries the user's own deliberately-unstaged edits, and removing the NUL bytes themselves is a source change this docs-only story is forbidden to make (AC #3). Two separable follow-ups: add the second filename to that bullet, and strip the NUL bytes from both files so the hazard stops existing.

- **The README documents Codex setup for a runtime the workspace has retired.** `## Codex Setup` (~57 lines) still calls Codex "the primary Cortex runtime" and carries `~/.codex/config.toml` wiring plus a `~/.codex/AGENTS.md` instruction, while the workspace `CLAUDE.md` records that this is a Claude-Code-only workspace and `AGENTS.md` files are retired. `cortex doctor` already states it does not inspect Codex wiring. Left alone deliberately: Story 2.7 is a repositioning pass, and deciding whether Cortex still supports Codex is a product call, not a docs edit. Whoever picks it up should decide support first, then edit — the section is either stale or load-bearing, and the docs cannot answer which.

- **`package.json`'s description still carries the pre-2.7 category framing.** `"description": "Working memory for AI agents — persistent cognitive state per project"`, with `claude-code` among the keywords. That string is the npm registry front page — in practice the highest-traffic copy of the positioning Story 2.7 exists to change, and it now disagrees with both the README lead and `CLAUDE.md`. Not fixed here on purpose: AC #1 names "the README", and this story's own constraints drew the no-touch line at `package.json` to keep the "no behavior changes" claim (AC #3) checkable by a blunt `git diff --stat` rule. Registry copy is also a publishing decision rather than a docs edit. One line, no behavior risk, but it wants an owner's call — surfaced by the acceptance-audit layer of this story's review.

## Deferred from: code review of 3-6-report-the-pl (2026-08-02)

- getSessionLedgerTotals builds one SQL variable per tree session with no count guard; a tree past SQLITE_MAX_VARIABLE_NUMBER (32766) throws 'too many SQL variables'. Structurally unreachable at today's depth-2 trees (sole caller passes one primary + direct children). Revisit if session nesting deepens.

## Deferred from: story-4.5 (2026-08-03)

- ~~**B-4 (≤500 ms p95 through the installed hook) is breached…**~~ **SUPERSEDED by the review round's entry below (2026-08-03):** this entry mis-scoped the budget — PRD §10's B-4 covers only the non-substituting path (`Edit`, `Write`, `Bash`, `Agent`), so the Read-path numbers cited here were being reported against a budget that does not govern them; the budget that does (B-4a end-to-end) is the subject of the open ruling. The underlying diagnosis stands and is re-recorded below with corrected scoping: the hook's three `jq` invocations (median 278.7 / p95 439.5 ms in isolation) dominate every path, and **merging the two setup `jq` calls into each branch's single one is worth ~180 ms per tool call on every budget** — its own change, touching every branch and the `capture-hook` structural tests.

- **AD-16 eligibility is a deliberate subset and can be widened without a schema change.** The hot path can only prove "this scope's published primary read it as itself", so a subagent's *own* earlier read is not refunded to it (story F2). The widening: have `collectIndexRecords` emit the **tree-root primary** session id in index column 5 instead of the recording session's id (a join through `sessions`), after which `record.agent_id == payload.agent_id` becomes provable and AD-16's full ancestor rule is expressible in bash. Deferred because it changes the documented meaning of an index column Story 3.2 specified — enumerate every consumer of `parseIndexLine(...).sessionId` with `find_referencing_symbols` first, and note the index is a projection so no migration is needed, only a rebuild.

- **`mergeIgnoreEntries` writes a fresh `# Cortex runtime artifacts` header on every run that adds entries.** Observed on this story's `cortex install`: the checkout's `.gitignore` now carries that comment twice, with `.cortex.db-wal`/`.cortex.db-shm` under the first and `.cortex.substitution`/`.cortex.turn-reads` under the second. Cosmetic and idempotent-per-entry (nothing is duplicated), but it accumulates one header per future story that adds a runtime artifact. Pre-existing in `src/query/install.ts`, surfaced by this story because it is the first to add entries since the header was introduced.

## Deferred from: story-4.3 (2026-08-03)

- **Search offers and `unrealized` accounting are not wired (D7).** Story 3.5's `read_offer` machinery is read-refund-specific (`offer:read`). A `no-matches-at` answer the agent reads and then re-searches anyway is a real adoption fact, and `assertCreditIsEvidenced` already models the `search` evidence shape (result count) — but wiring `offer:search` is accounting surface Story 4.3's ACs do not name, and it was deliberately left out to keep the change reviewable. Consequence, stated so nobody mistakes it for a defect: the negative cache appears in neither `Saved` nor `Unrealized` in `cortex stats`; its value is invisible to the P&L until this lands.

- **The certification window cannot see writers that leave no captured event, and that residual is irreducible at this design's flush-time census.** Narrowed as far as the capture stream allows (any command at-or-after the search, any backgrounded command in the batch whatever the order, any edit/write under the root, same-second ties, the live-spool peek) — but a rename performed by an MCP editing tool, an edit made in the user's editor, or a `git pull` in another terminal produces no spool line at all, so the flush certifies and the census fingerprints the post-change tree. Story 3.1's flush-window bound is the same shape, but there the consequence is a lost refund and here it is a false assertion (SM-C3). Two candidate closures, both bigger than this story: fire capture on every tool so unknown tools emit a disqualifier line (costs a hook spawn per tool call — measured ~500 ms on this platform, so it needs the three-jq→one-jq merge first), or move the census into the search's own turn (blocked today: N-4 forbids Node per tool call and bash cannot hash a tree).

- **`CERTIFIABLE_PATTERN` keeps the negative cache to literal-ish patterns, which is most of its potential hit rate.** Measured on this repository's live spool: real agent searches are frequently regex (`a|b|c` alternations, anchors), all of which the gate refuses. The gate exists because pre-2.1.208 hosts answer invalid regexes zero-shaped; a host-version check (the payload carries no version, but `claude --version` is cheap at install time) could widen the class on hosts known to report regex errors properly.

- **A pattern-only search (no `path`) roots at the session cwd, which for a repo-root session meets `node_modules` and overflows the census ceilings** — so the most common search shape is never cached. Design-accepted (the README says so), but nothing measures it, so the dead weight is invisible. A per-scope counter of "certified vs refused, by reason" would make the hit rate observable before anyone tunes the ceilings.

## Deferred from: code review of story-4.5 (2026-08-03, three layers)

- **The credit's evidence stops at the client boundary: no host acknowledgement exists for `updatedToolOutput`.** The credit line is emitted only after every verification gate passed on evidence in hand, but the hooks API returns nothing, so a Claude Code build that silently stopped honouring `hookSpecificOutput.updatedToolOutput` (the current shape is itself undocumented, proven by manual probe) would deliver full files while `saved` rows still book. Unfixable client-side; documented in README and CLAUDE.md. If the hooks API ever grows an acknowledgement or a version field in the payload, gate the credit on it.

- **Two concurrent Claude Code windows sharing one project root share `.cortex.state`, so the second window's SessionStart owns the published primary.** Window A's reads then satisfy the session gate with window B's session id — attribution drift inherited from the session model (Story 3.3 reports through the same shape), with substitution bounded by the eligibility rule (any command in the batch disqualifies) and AC #4's escape hatch. A real fix is per-window session identity, which is scope-layer work, not a substitution patch.

- **Astral-plane paths never substitute**: bash `${key,,}` corrupts the lead byte of a 4-byte UTF-8 sequence, and `grep -F` was measured missing even an uncorrupted needle for such paths. Silent under-refund only; noted in the hook. Revisit if a real repository ever surfaces it.

- ~~**B-4a needs a PRD ruling (owner: ShuromiU) — see the open sprint action item.**~~ **RULED 2026-08-03 (ShuromiU): re-based end-to-end with the structural clause primary** — miss ≤600 ms / hit ≤800 ms p95 through the installed hook on a quiescent reference platform, with the structural clause (no Node, index-only lookup, grep+wc+sha256sum spawn budget, hash strictly after both gates) as the normative, CI-pinned half. The quiescent measurement (510.9 / 612.0 / 671.7 ms) passes with 15–19% headroom; recorded as PRD §10's third dated amendment, epics.md AC #5 re-amended, Story 4.5 `review → done`. Original entry, kept for the diagnosis: as written (miss ≤100 ms / hit ≤300 ms end-to-end p95 through the installed hook) it measures 510.9 / 612.0 / 671.7 ms and is structurally unreachable on the reference platform: one bash spawn (p95 83.9 ms) plus one `jq` (~100 ms) exceeds the miss budget before any Cortex logic runs. Substitution's own marginal is +1.9 ms (miss) / ~+65 ms (hit). The B-4 breach previously recorded here was mis-scoped — PRD §10's B-4 covers only the non-substituting path — and **the three-jq→one-jq merge (~180 ms per tool call, benefits every budget) remains the highest-value latency fix on the hook; it stays open, deliberately not a condition of the ruling.**

## Deferred from: story-4.4 (2026-08-03)

- ~~**B-3 is unmet for the command ledger and needs a PRD ruling.**~~ **RULED 2026-08-03 (ShuromiU): the measured figure is ACCEPTED.** The query is user-initiated, runs once, sits on no hot path and stands between nobody and their work, so a tighter number would be bravado rather than value — explicitly conditioned on never trading accuracy for speed, so the input set stays the full git-listed working tree. Cortex reported that the dominant cost is irreducible (one `git ls-files` subprocess, 31–80 ms, against a ~40 ms process-spawn floor) and that the only accuracy-preserving lever left saves 13–19 ms on the MISS path alone; it was not taken. Original diagnosis, kept: Measured: `computeGitListedCensus` over this repo is 65.9 / 68.7 / 80.0 ms (min/median/max), of which one `git ls-files` subprocess alone is 31–80 ms; `git status --porcelain` is 55.1 ms; a raw hash of the tracked tree is 13.5–19.2 ms. B-3 (PRD line 683) allows 20 ms p95 for tool-output queries. **Deliberately not pre-empted at implementation time**: the only way to fit the number is to fingerprint less of the tree, which is exactly how a false pass is manufactured (SM-C3). The correctness ACs are unconditional; the test asserts boundedness and non-degradation with table size, not the budget. Candidate shapes for the ruling: re-base B-3 per surface as B-4a was re-based; exclude a git-backed state check from B-3 and give it its own budget; or accept a cheaper log-based check (Cortex has seen every edit it captured) at the cost of missing out-of-band writers.

- **The dependency and environment surface is outside the fingerprint, by construction.** `git ls-files --cached --others --exclude-standard` is the file universe, so `node_modules`, build output, the Node version, and environment variables are all invisible. The realistic dependency-change path does move the fingerprint (`package-lock.json` is tracked), and AC #4 refuses to record whenever any other command ran in the window — which is how `npm install` is caught in practice — but a hand-modified `node_modules`, a Node upgrade, or an env change between the record and the query can leave a stale `passed-at` standing. This is the PRD §16 assumption for FR-15 arriving exactly as anticipated ("if environment or dependency state can change results without either changing, the key is incomplete and tool-output caching must be narrowed"); the narrowing is AC #3. Closing it properly means fingerprinting the resolved dependency tree and the interpreter, which is a bigger surface than this story.

- **`npm run <script>` trusts the script NAME to describe the script.** The body lives in the user's `package.json` and is invisible to the gate, so a `build` script that deploys, or a `test` script ending in `|| exit 0`, is indistinguishable from an honest one. Bounded by the fact that the record only ever asserts "this command succeeded", which stays literally true — and a test script that lies about its own exit status fools CI identically. Reading `package.json` and gating on the resolved command line would close it; that is a new input this story does not take.

- **In effect only the LAST command of a window certifies**, because any other command at-or-after a candidate disqualifies it (AC #4). That is correct — a later command can install, build, or write anything — but it means a turn running build, then lint, then tests as three separate calls records only the tests. An `&&` chain records all three, which is the shape this repo's own verification block already uses, so the practical loss is smaller than it reads. A per-element classifier that proved an intervening command could not touch the tree would widen it, and would need to be as conservative as this gate is.

- **Command outcomes are not wired into the P&L (the same D7 gap as Story 4.3).** No `offer:command` exists, so a `passed-at` answer the agent reads and then re-runs anyway appears in neither `Saved` nor `Unrealized` in `cortex stats`. The value of the whole feature is invisible to the report that judges whether Cortex earns its place. Wiring both offer kinds together is the natural shape, and belongs with whichever story next touches the ledger surface.

- **The transcript is a Claude Code artifact.** Any host that does not provide `transcript_path`, or provides one in another format, records no command outcomes at all. This degrades safely and reports itself (`cortex doctor` → `Command outcomes`), but the capability is host-specific in a way no other Cortex feature is. If a second host ever matters, the oracle needs a per-host adapter rather than a widened parser.

## Deferred from: code review of story-4.4 (2026-08-03, three layers)

- **The feature is correct and nearly inert until verification commands are run BARE.** Measured over 1,413 real Bash calls in this repository's transcripts: **4** were cacheable, **0 of 92** in the most recent window. The cause is not the safety gate being wrong — it is that nearly every command an agent runs is piped through `tail`, and a pipeline reports its LAST stage's status, which is untrustworthy by construction (reproduced: a failing suite piped through `tail -6` exits 0 and the hook fires). Closing this is a working-habit change: run `npm test`, not `npm test 2>&1 | tail -5`. Recorded in README and CLAUDE.md rather than left to chance, because a capability nothing reaches is the "shipped is not used" gap the standing epic rule exists to catch.

- **Only the LAST command of a window can certify.** Any other command at-or-after a candidate disqualifies it, so a turn running build, lint and tests as three separate calls records one. An `&&` chain records all three and is the shape this repo already uses, so the practical loss is smaller than it reads — but a classifier that could PROVE an intervening command did not touch the tree would widen it, and would have to be as conservative as the gate it relaxes. Note the coupling: the record-count dedup in `captureCommandOutcomes` is unreachable while this rule holds (its mutation survives the campaign, deliberately and documented in place) and becomes load-bearing again the moment it is relaxed.

- **Unknown tool names are not treated as tree mutators.** `isMutatingToolName` lists the shell, subagents, the editors and the symbol-refactor MCP names. Treating every unrecognised tool as a mutator would disqualify on each read-only MCP call and leave nothing recordable at all; treating none as mutators is the residual Story 4.3 already accepted. A new MCP editing tool would be invisible to both the spool matcher and this list until added to one of them. The honest closure is a host-provided "this tool writes" signal, which does not exist.

- **A submodule is fingerprinted by presence, not by revision.** A gitlink is recorded as a distinct entry so one submodule no longer kills the capability for the whole repository, but its commit is not read — a bump changes the superproject index, which `git ls-files --cached --others` does not report. Closing it means a second git call per census, which the B-3 ruling deliberately did not buy.

- **Command outcomes are still not wired into the P&L (the same gap as Story 4.3).** No `offer:command` exists, so a `passed-at` the agent reads and then re-runs anyway appears in neither `Saved` nor `Unrealized` in `cortex stats`. Given the eligibility measurement above, that accounting is now the only way to tell whether this feature ever pays for itself.

## Re-scope of story-4.4 (2026-08-03, ruling by ShuromiU)

- **FR-15 (tool-output query) is WITHDRAWN from R1.** Three review layers over two rounds found six reachable routes to a false `passed-at` — stale re-certification on a quiet turn; a failed tree-mutating sibling that leaves no spool line; a command directory narrower than its real input set; an empty fingerprint that matches forever; npm workspace/prefix flags running a different script body than the gate reads; and a concurrent second window whose writes this session never sees. Three of them survived a dedicated fix round, and two were introduced BY it. Every fix narrowed the recordable set, against an eligibility measured at **4 of 2,051 real commands, all four the developer's own probes and zero organic**. The PRD's own §16 assumption for FR-15 anticipated this in writing; the narrowing reached zero. The code, its 143-test suite and the three review reports are preserved in the session scratchpad so re-opening starts from evidence. **Re-opening requires**: a way to observe writes this session did not make (concurrent windows, subagents, out-of-band editors), which no host signal currently provides.

- **`classifyCommand` maps every `npx …` to the generic `npm` category**, and `writeCommandEpisodes` only writes episodes for test/build/git — so `npx vitest run` and `npx tsc --noEmit`, this project's actual verification commands, now get an exit code and a `command_run` but no `command_failure` episode. Pinned by a test in `tests/command-outcomes.test.ts` rather than left implicit. Widening the classifier changes episode text and the consolidation path for every existing caller, and `tests/redact.test.ts:23` deliberately asserts the current mapping, so it belongs to whichever story next owns episode shaping.

- **Command outcomes are attached by command TEXT, not by tool-call id.** The spool line carries no `tool_use_id`; adding one is a hook-template change and a machine-wide reinstall. Two runs of the same text with conflicting outcomes in one window are marked ambiguous and attach nothing. Adding the id would make the pairing exact and is the natural follow-on for any story that already needs a hook change.

- **Failure synthesis is bounded to the flush window** (only failures at-or-after the earliest spool entry in the batch). A turn whose only activity was a failed command — no spool entries at all — synthesizes nothing. Rare, a miss rather than a wrong record, and closing it needs the per-scope watermark the withdrawn cache used, which carried its own defects (a future-dated stamp killed capture permanently while `doctor` reported health).

## Round-3 corrections to the story-4.4 entries above (2026-08-03)

- **The two 4.4 sections ABOVE the "Re-scope" section describe the WITHDRAWN cache.** They name `captureCommandOutcomes`, `isMutatingToolName`, `computeGitListedCensus`, the AC #3/#4 gates and a `Command outcomes` doctor check that did not then exist. All of those symbols are absent from `src/`. They are kept for history; **only the "Re-scope of story-4.4" section and this one describe live carry-forward.**

- **CORRECTION — the doctor check now exists.** Those sections asserted the capability "reports itself (`cortex doctor` -> `Command outcomes`)" while nothing implemented it, and the story claimed AC #8 MET on that basis. It is implemented as of the round-3 fixes: a conditional `command-outcomes` row reading `cmd_outcome_scan` from meta, silent on a project where nothing has run, warning when commands are recorded but no scan ever ran or when the transcript is unusable.

- **CORRECTION — README was never modified.** Task 12 and the earlier deferred-work note both claim the `npx` episode gap was "recorded in README and CLAUDE.md rather than left to chance". `README.md` is byte-unmodified and contains no command-outcome content. The gap is recorded in `docs/invariants.md`, in this file, and pinned by a test.

- **The synthesis window is gone, replaced by `tool_use_id` identity.** The earlier note here said failure synthesis is "bounded to the flush window ... a miss, never a duplicate". Round 3 reproduced it as **both**: duplicates by two routes, and a systematic miss of the first failure of every turn (the transcript stamps emission, the hook stamps completion). Superseded — synthesis now writes each failure exactly once by id, runs once per flush in its own transaction, and is capped at 50 rows per flush with the count reported in `SpoolFlushResult.synthesized`.

- **Five live stores carry an orphaned `command_outcomes` table** (`cortex` with 2 rows, plus `app`, `repo-c`, `Section-Sixteen`, `ShuromiU`), created by the withdrawn build before the ruling. Nothing reads, writes or prunes it now. Harmless — `schema_version` stays 6 so nothing refuses to open — but it is undocumented residue. Dropping it is a migration and AD-11 spends one `SCHEMA_VERSION` increment per release, already committed for this one, so it waits for whichever release next needs a schema change. Do not "clean it up" outside a migration.

- **Attaching outcomes by `tool_use_id` instead of command text** is still the natural follow-on for any story that already needs a hook-template change. It would remove the `ambiguous` case entirely and would let `bg` and `agent_id` lines be paired correctly rather than merely refused.

## Deferred from: code review of story-5.2 (2026-08-07)

Three layers; the seven headline findings were re-verified against the built
`dist/` before any was acted on. Three HIGH defects and eight smaller ones were
fixed in the story. These are the rest.

- **Reflex dedupe state files now proliferate per subagent, unbounded.** `statePath` (`src/query/reflex.ts`) writes `os.tmpdir()/cortex-reflex-<sessionKey>.json`, and this story re-keyed that id from the primary to the child — so a fan-out of N subagents leaves N files where one used to be. Measured: 5 subagent edits → 5 files. Text search finds no `unlink` or `rmSync` in `reflex.ts` or `gc.ts` (grep, not symbol-verified — there is no symbol to enumerate). At this repository's own cited rate, 56 dispatches in four days, that is roughly 14 new temp files per project per day, forever. This story bounded the growth of the table it created — the `content_digests` lesson — and created this growth without bounding it, which is the same mistake one layer over. The files are tiny and the OS reclaims `TMPDIR` eventually, which is why it is deferred rather than fixed under time pressure. **Owner: whichever story next touches `src/query/reflex.ts`**; the fix is either a `gc.ts` rule over the directory or an ended-session sweep.

- **The first-fire marker repair is FORWARD-ONLY.** Moving `recordSubagentStart` above the session create fixes stores whose marker is not yet set. Every store that already stamped it under Story 5.1 keeps a marker one millisecond past its first child, so `doctor` keeps reporting one child too few — permanently, and as a `pass`, because `fires > children` is the quiet side of the comparison. Eleven live stores on this machine. Deliberately not migrated: the error is exactly one, and rewriting a marker in every store to correct a display count is an unattended data change of the shape Epic 4's rollout warns about. **Owner: ShuromiU**, if the count ever matters enough to be worth a migration.

- **`subagent_dispatches.scope_key` is written on every capture and read by nothing.** `certify_refs("subagent_dispatches")` returns 7 union sites (lspOnly 1, textOnly 5) and no query selects or filters on it. It satisfies "everything is scope-keyed" and is the reason `captureDispatch` requires an active primary — a requirement that costs a real miss (no capture, no brief) purely to populate the column. Kept because it is the natural key for any per-scope reporting a later story adds, and dropping a column is a migration. **Owner: whichever story first reports on dispatches per scope** — either use it or remove it and the guard together.

- **There is no `reflex-matcher` doctor row.** `capture-matcher` covers `PostToolUse` and this story added `dispatch-matcher` for `PreToolUse (subagent dispatch)`, but nothing checks that the reflex wiring's matcher still covers `Edit|Write`. Pre-existing, and newly reachable: with two wirings sharing `PreToolUse`, a hand-edited or split-and-re-merged record can leave `reflect-pre` on the wrong tool with every row green. **Owner: whichever story next changes `REQUIRED_WIRING`** — the honest fix is one matcher check driven by `REQUIRED_WIRING` rather than three hand-written rows.

- **A backwards clock jump larger than the GC window re-opens old orphans.** ISO-8601 string comparison is sound (`toISOString()` is fixed-width UTC) and forward skew fails safe. Backward skew does not: set the clock back a day or more — VM resume, a large NTP correction — and yesterday's unconsumed capture satisfies `captured_at > cutoff` again before the daily GC sweep runs. The 2026-08-07 refusal rule narrows the damage to the case where exactly one stale capture matches. Reproducible only with a deliberate clock change. **Owner: unassigned**; recorded because the pairing horizon is the only correctness bound on that table.
