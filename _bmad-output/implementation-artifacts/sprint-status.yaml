# generated: 2026-07-24
# last_updated: 2026-07-24
# project: cortex
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# RELEASE SCOPE:
# ==============
# Every epic below is release R1 (Cortex Context Economy). Source of truth for
# scope, acceptance criteria and dependency ordering is:
#   _bmad-output/planning-artifacts/epics.md            (6 epics, 27 stories after the 2026-07-28 amendment)
#   _bmad-output/planning-artifacts/replan-r1-2026-07-28.md  (competitive-research course correction)
#   _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-24.md
# Epic order is dependency-driven, not thematic. Do not reorder epics without
# re-reading the readiness report — Epic 3 exists before Epic 4 so the cache is
# measurable on arrival, and Epic 5 depends on Epic 0's session identity fix.
# 2026-07-28 amendment: stories 4-1, 4-2, 5-4 withdrawn from R1 (FR-10/11/20
# back to PRD roadmap); Epic 4 execution order is 4.5 -> 4.3 -> 4.4 -> 4.6;
# story 2-7 (docs repositioning) added.

generated: 2026-07-24
last_updated: 2026-08-03  # 4.3 done (3a46d12); 4.4 dev complete -> review
project: cortex
project_key: NOKEY
tracking_system: file-system
story_location: '{project-root}/_bmad-output/implementation-artifacts'

development_status:
  # Epic 0 — Session Identity Correction (defect fix; AD-9)
  epic-0: done
  0-1-resolve-sessions-by-agent-identity: done
  0-2-carry-agent-identity-through-the-capture-spool: done
  epic-0-retrospective: optional

  # Epic 1 — Trust Activation (FR-1..FR-4, FR-44)
  # EXECUTION ORDER: run 1.5 FIRST, then 1.1 → 1.4. Story numbering is unchanged;
  # only the order of work differs. 1.5 lands the eval gate and depends on nothing
  # in 1.1–1.4, while 1.2 and 1.3 change rendered retrieval output and will move
  # output_tokens. Gate first, and each delta is attributable to the story that
  # caused it. Gate last, and the accumulated drift arrives as one red build with
  # no way to tell which change bought what.
  epic-1: done
  1-1-detect-contradictions-at-note-write-time: done
  1-2-render-contested-items-in-retrieval: done
  1-3-surface-rejected-alternatives-at-recall: done
  1-4-auto-demote-superseded-decisions-without-resolving-contests: done
  1-5-gate-retrieval-quality-in-ci: done
  epic-1-retrospective: done

  # Epic 2 — Operability (FR-21..FR-26)
  epic-2: done
  2-1-list-and-inspect-memory: done
  2-2-correct-and-delete-memory: done
  2-3-diagnose-the-installation: done
  2-4-install-in-one-idempotent-command: done
  2-5-relocate-the-store-out-of-the-project-root: done
  2-6-bound-the-write-ahead-log: done
  2-7-reposition-the-docs: done # added 2026-07-28 (replan); docs-only, no dependencies
  epic-2-retrospective: optional

  # Epic 3 — Read Ledger and Token P&L (FR-5..FR-9; 3.2 carries AD-3, no FR)
  epic-3: done
  3-1-capture-content-digests-on-read: done
  3-2-build-the-flat-digest-index: done
  3-3-answer-the-read-ledger-question: done
  3-4-surface-the-read-ledger-in-the-session-brief: done
  3-5-account-for-tokens-injected-and-saved: done # 1ddd043 + reconciliation 2d2b2e4 (fixed an overstatement in 1ddd043's own report)
  3-6-report-the-pl: done # epic title: "Story 3.6: Report the P&L"
  epic-3-retrospective: optional

  # Epic 4 — Content Cache (FR-12..FR-16, FR-6 substitution; FR-10/FR-11 withdrawn 2026-07-28)
  # EXECUTION ORDER (2026-07-28 replan): 4.5 FIRST, then 4.3 -> 4.4 -> 4.6.
  # Story numbering unchanged (Epic 1 precedent). 4.5 is time-sensitive
  # (platform hook shipped in CC v2.1.121; competitors ship partial mechanisms)
  # and was BLOCKED on the B-4 re-base action item before create-story — resolved
  # by the 2026-08-02 ruling (bc19e92): B-4a split by outcome, miss <=100 ms /
  # hit <=300 ms p95 through the installed hook. 4.5 create-story ran 2026-08-02.
  epic-4: in-progress
  # 4-1-derive-file-cards-deterministically: withdrawn from R1 2026-07-28 (FR-10 to roadmap; see replan-r1-2026-07-28.md)
  # 4-2-serve-file-cards-with-a-freshness-verdict: withdrawn from R1 2026-07-28 (FR-11 to roadmap; see replan-r1-2026-07-28.md)
  4-3-remember-searches-that-found-nothing: done # 3a46d12; dev + three-layer review reconciled 2026-08-03 (created same day). 34 findings; all three layers converged on the same SM-C3 class from four routes (wrong root / wrong zero / wrong file universe / invisible writer) - all four closed. THE PAYLOAD PROBE RAN (this session is itself a live Claude Code session; hook stdin dumped for four real searches, restored byte-identically): tool_response is always an object keyed on numFiles/filenames - four of six shipped markers were fiction, capture had worked on one lucky guess. Zero now requires no-positive-evidence; root is the real search root; census/search universe parity enforced via git check-ignore; backgrounded cmds and NotebookEdit/symbol-refactor tools disqualify. Live E2E proven twice with REAL session searches (no-matches-at -> miss on change -> honest re-validation). 1533 tests, gate 9/9, doctor 17/17.
  4-4-remember-the-outcome-of-expensive-commands: done # ROUND 3 (2026-08-03) reviewed the re-scoped surface, which had never been reviewed: four HIGH data-correctness defects, all reproduced — duplicate synthesis (orphan claim + whole-second window, one failure as 3 rows), never-executed commands recorded as real failures (5 of 130 measured, incl. one the USER DENIED), backgrounded launches recorded as exit 0 (a false "tests passed" — the SM-C3 failure from the one direction the withdrawal did not close), and a subagent success stamped with the parent failure. Plus five false claims in shipped prose, incl. a code comment the suite own test contradicted, and AC #8 claimed MET but never implemented. All fixed: synthesis keyed on tool_use_id (identity, not a time window), the exit line required as the executed-witness, bg and agent_id lines refused, doctor command-outcomes row added. 1583 tests, gate 9/9, 9/10 mutations killed. Earlier: RE-SCOPED by ruling (ShuromiU) 2026-08-03 after two review rounds. SHIPPED: the command-outcome oracle only — Cortex can now see whether a command passed or failed, which was never observable (measured: 4,881 command_runs, 2 exit codes, both fixtures; command_failure and test_cycle episodes had NEVER fired). Live: 2 -> 20 outcomes after one flush, including a command that fires no hook at all. WITHDRAWN: the FR-15 cache ("would this still pass?") — three layers over two rounds found SIX reachable routes to a false passed-at, three surviving a dedicated fix round, against eligibility measured at 4 of 2,051 real commands (all four the dev own probes, zero organic). Every safety fix narrowed the recordable set further; the PRD §16 assumption arrived exactly as written and the narrowing reached zero. Precedent: FR-10/FR-11 withdrawn from this epic 2026-07-28. Withdrawn code + all three review reports preserved in the session scratchpad. 1558 tests, gate 9/9, doctor 17/17. Original create-story note follows: ACs RE-BASED against measurement, not the epic text: the Bash PostToolUse payload carries NO exit code in any arm of the shipped lookup (5 payloads probed; tool_response is exactly stdout/stderr/interrupted/isImage/noOutputExpected), and a host-failed command fires NO PostToolUse at all (exit 3/7, process.exit(2), bare failing vitest -> zero payloads). Corroborated in production: 4,881 command_runs, 2 exit codes (both fixtures), 0 stdout_tails, 0 test_cycle episodes - both outcome-gated episode writers have never fired. Outcome is instead taken as EVIDENCE from the session transcript's structured `is_error` boolean paired by tool_use_id (verified against the probes), read tail-only in the cold-path flush, so failures are recordable too and the hot path gains nothing. SM-C3 trap reproduced live: `npx vitest run <failing> | tail -6` exits 0, the hook FIRES, and stdout says "1 failed" - hence AC #2's structural gate (refuse | || ; & backtick $( ; allow && chains). Input set excludes git-ignored paths (node_modules here is 4,833 files vs a 2,000 ceiling). B-3 at risk and deliberately NOT pre-empted - see the epic-4 action item. DEV RESULT: transcript_path measured PRESENT in the hook payload, so no hook-template change and no machine-wide reinstall are needed for capture. A gap found while PROVING the failure case: a host-failed command writes no spool line, so the pass had to move from processClaimFile up to flushSpool and run unconditionally, or failure capture would have been dead on arrival. Live E2E proven 7/7 incl. a real failure recorded from a command that fired no hook, and the piped failing suite refused. B-3 MEASURED AND NOT MET: computeGitListedCensus 65.9/68.7/80.0 ms vs a 20 ms budget - correctness ACs shipped unconditional, number carried to the ruling. 1652 tests, gate 9/9, doctor 18/18.
  4-5-refund-redundant-reads-through-verified-substitution: done # e7ae876; B-4a ruled 2026-08-03 (re-based end-to-end, structural clause primary) — AC #5 latency half met as re-based, story closed
  4-6-bound-and-evict-the-derived-cache: backlog
  epic-4-retrospective: optional

  # Epic 5 — Subagent Memory (FR-17..FR-19; depends on Epic 0; FR-20 withdrawn 2026-07-28)
  epic-5: backlog
  5-1-link-subagent-sessions-to-their-parent: backlog # reduced scope: Epic 0 landed agent_id child sessions; remaining is SubagentStart proactive path + disengaged guard
  5-2-brief-subagents-automatically: backlog
  5-3-write-subagent-conclusions-back: backlog
  # 5-4-record-sibling-claims-during-fan-out: withdrawn from R1 2026-07-28 (FR-20 to roadmap; Agent Teams ships lock-and-claim natively; see replan-r1-2026-07-28.md)
  epic-5-retrospective: optional

action_items:
  - epic: 1
    action: "Re-base B-4 (hook latency budget) against the Windows/Git Bash platform — PRD amendment; ~400 ms measured vs <=15 ms budget; adopted from Epic 0's never-run retrospective. 2026-07-28: now also BLOCKS Story 4.5 creation — B-4a (<=100 ms substitution path) inherits the same platform reality"
    owner: "ShuromiU (PRD decision)"
    status: done  # 2026-08-02 ruling (ShuromiU): B-4 re-based to <=500 ms p95 through the installed hook, structural clause (pure bash, no Node, no SQLite) primary; B-4a split by outcome — miss <=100 ms / hit <=300 ms p95. Recorded as dated amendments in PRD section 10; epics.md 4.5 AC updated; alternatives (flat ~250 ms, MCP-server listener, defer FR-6) recorded in the amendment note. Story 4.5 create-story is unblocked.
  - epic: 1
    action: "Fold the two CLI resolve defects (note-resolve conflict clearing; contested --subject refusal) into Story 2.2's scope at create-story time"
    owner: "create-story (Story 2.2)"
    status: done
  - epic: 1
    action: "Decide contested-pair deletion semantics before Story 2.2 implements — deleting one side must not leave a dangling [contested] survivor"
    owner: "Winston (Architect) + ShuromiU"
    status: done  # decided at 2.2 create-story: deleting one side clears the contest for the subject, in the same transaction
  - epic: 1
    action: "Codify the mutation-campaign checklist in project-context.md: mutate source not dist, EOL-adaptive anchors, prove every mutation applied via red-check"
    owner: "Amelia (Developer)"
    status: open
  - epic: 1
    action: "Add header/full_state assertion hooks to the eval harness so state-surface regressions gate"
    owner: "Amelia (Developer)"
    status: done  # Story 3.4 shipped it: suites carry `surfaces` entries over brief/header/full_state (expect_contains/expect_excludes/max_tokens/budget), validated at load, failing on their own terms, with unit tests for the mechanism itself. brief-surface asserts on all three surfaces. Found along the way: superseded-history could not exercise the brief's superseded guard at all (its item is `cold`, filtered before the guard) so brief-surface seeds it `warm`; max_tokens:150 was decoration against a 77-token brief so a binding `budget` was added; and a first baseline silently baked in a failing fixture, now reported. Stated limit: the FR-7 read-ledger line itself is excluded from the gate (includeReadLedger:false) because a seeded store cannot stage on-disk files.
  - epic: 3
    action: "content_digests has no GC rule (certify_refs: zero hits in src/db/gc.ts). One row per path per scope and every branch mints a scope, so it grows monotonically. Fold into Story 4.6, which owns GC and bounds."
    owner: "create-story (Story 4.6)"
    status: done  # Story 3.2 added pruneContentDigests (CORTEX_GC_DIGEST_DAYS, default 60, dry-run by default); 4.6 inherits a rule rather than a table that never had one
  - epic: 3
    action: "AC #5 (<=400 bytes/file) holds to p90 but FAILS for this repo's longest real paths (135ch -> 417.8 with a real 74-char scope key). Fix is storing the path relative to the scope root; adopt in Story 3.2 when the flat index format is defined (AD-3)."
    owner: "create-story (Story 3.2)"
    status: done  # 3.2 stores scope-root-relative keys: 135ch now 376.8 PASS, first breach moved 130 -> 152. STILL breached at 135 when agent_id is populated (subagent reads); reducing further means shortening scope_key, a scope-layer change
  - epic: 3
    action: "Under the P-5 refusal the spool never drains and the capture hook keeps backgrounding a no-op flush-spool past 256 KiB — an N-4 violation in exactly the state P-5 creates. Decide whether to park the spool or mark the project refused."
    owner: "ShuromiU + Amelia"
    status: open
  - epic: 3
    action: "content_digests.session_id is ON DELETE CASCADE, binding a scope-wide change-detection fact to whichever session read last. Latent (no DELETE FROM sessions in src/) but a future session GC would destroy facts other sessions need."
    owner: "create-story (Story 4.6)"
    status: open
  - epic: 3
    action: "openProjectStore refuses a newer store AFTER resolveProjectStore has already run the legacy VACUUM INTO, so the copy lands without root_commit_oid or migration provenance. Narrow but real; reorder the version check ahead of migration."
    owner: "Amelia (Developer)"
    status: open
  - epic: 3
    action: "AC #5 remains breached for SUBAGENT reads of the longest paths: agent_id shares the row budget, so first breach is 135ch with a 17-char agent id — exactly this repo's maximum. Pinned by test. Fixing it means shortening scope_key (scope-layer change), not the digest table."
    owner: "Winston (Architect) + ShuromiU"
    status: open
  - epic: 3
    action: "normalizeFilePathKey folds case with JS toLowerCase(), whose full-Unicode folding creates collisions the OS does not: on NTFS, `i̇stanbul.ts` and `İstanbul.ts` coexist as two files and fold to ONE ledger key. The docstring justifies folding on the grounds that Makefile/makefile cannot coexist on a case-insensitive filesystem, which is true for ASCII only. Degrades safely (a wrong unchanged-since needs identical bytes, in which case the refund is content-correct). Owned by src/scope/keys.ts (Story 3.1/3.2), surfaced by 3.3's review; fixing it changes stored keys and needs a migration."
    owner: "Winston (Architect) + ShuromiU"
    status: open
  - epic: 3
    action: "normalizeReadLedgerPaths filters entries on `p.trim().length > 0` but returns the UNTRIMMED value. On win32 the filesystem canonicalises a trailing space or dot away, so ` src/a.ts ` names a file that WAS read but misses the key lookup and answers `unread`. Miss direction only (a lost refund, never a false one), and trimming unconditionally would break POSIX filenames that legitimately end in a space. Left as-is deliberately; revisit if Story 4.5 makes lost refunds expensive."
    owner: "Amelia (Developer)"
    status: open
  - epic: 3
    action: "doctor's `Hook interpreter` check reports `bash -> C:/Program Files/Git/usr/bin/bash.exe` as PASS, but that binary is the BARE msys bash, not the Git/bin wrapper that sets up a POSIX PATH. Measured with the full Windows PATH: through usr/bin/bash.exe the shell cannot see grep, date, tr or wc (jq IS found, since it is on the Windows PATH, so doctor's separate jq check also passes), and the real cortex-capture.sh exits 0 writing NO spool line - AD-12 silent degradation hiding total capture loss. Through Git/bin/bash.exe the same call writes the record. doctor checks that the interpreter RESOLVES and that jq resolves; it never checks that the coreutils the script uses are reachable FROM the interpreter it named. Live capture on this machine currently works (spool is growing), so this is a latent diagnostic gap, not an outage. Fix belongs to FR-23 (Story 2.3's check), not to Epic 3."
    owner: "Amelia (Developer)"
    status: open
  - epic: 3
    action: "B-4a (<=100 ms) is unmet on this platform independently of Cortex: measured spawn floor ~39-42 ms, jq alone ~81 ms, a plausible 4.5 sequence 214.8 ms. Story 3.2 reports its lookup cost (41-100 ms over 1k-200k records) but does NOT claim the budget met. Blocks 4.5 with the existing B-4 amendment."
    owner: "ShuromiU (PRD decision)"
    status: done  # 2026-08-02: resolved by the same ruling as the epic-1 B-4 item — B-4a split by outcome (miss <=100 ms / hit <=300 ms p95 through the installed hook). The 214.8 ms measured sequence sits inside the hit budget; the grep-first miss path (41-100 ms) sits inside the miss budget at realistic index sizes.
  - epic: 4
    action: "B-4a needs a ruling: as written (miss <=100 ms / hit <=300 ms, p95 END-TO-END through the installed hook) it measures 510.9 / 612.0 / 671.7 ms and is structurally unreachable on the reference platform — one bash spawn (p95 83.9 ms) + one jq (~100 ms) exceeds the miss budget before any Cortex logic runs. Substitution's own marginal is +1.9 ms miss / ~+65 ms hit. Story 4.5's review round retracted its earlier marginal-vs-B-4 reading (PRD section 10 scopes B-4 to the NON-substituting path, so B-4a is the only budget governing Read). Candidate shapes for the ruling: re-base B-4a end-to-end with the structural clause primary (the B-4 precedent); define B-4a as marginal and widen B-4 to cover Read's base cost; or land the three-jq merge (~180 ms/read) first and re-measure. Until ruled, AC #5's latency half is reported NOT met; structural halves met and pinned."
    owner: "ShuromiU (PRD decision)"
    status: done  # 2026-08-03 ruling (ShuromiU): B-4a re-based end-to-end with the structural clause primary (the B-4 precedent, first candidate shape). Structural clause is the normative, CI-pinned half: no Node, index-only lookup, exactly one added spawn on a miss (grep -F -m1) and two more on a verified hit (wc -c, then sha256sum strictly after the size and eligibility gates). End-to-end ceiling prices the platform: miss <=600 ms / hit <=800 ms p95 through the installed hook on a quiescent reference platform at default size bounds; quiescent measurement 510.9 / 612.0 / 671.7 ms passes with 15-19% headroom, re-measured per hook change rather than CI-gated. Recorded as PRD section 10's third dated B-4a amendment; epics.md line 99 + AC #5 re-amended; story F5 + completion notes updated; Story 4.5 review -> done. Rejected: marginal-over-B-4 (widens B-4 to cover Read, dismantling the 2026-08-02 scoping); jq-merge-first (holds an AC hostage to an optimisation — the ~180 ms/read merge stays deferred on its own merits).
  - epic: 3
    action: "Story 4.5 must revise the stats saved-0 explanation (renderStatsReport, src/query/stats.ts): its wording asserts 'the mechanism ... is not shipped' — a global fact — while its keying is scope-local (scope saved == 0). The day substitution books savings in ANY scope, every other scope prints a false statement. Found by 3.6's review (edge+blind); a BINDS STORY 4.5 comment sits on the branch."
    owner: "create-story (Story 4.5)"
    status: done  # 2026-08-03: Story 4.5 D9 shipped it. The branch now states only what the reported scope can observe — "read substitution is available but not enabled here (cortex substitution on)" or "no verified savings in this scope yet" — never the global "is not shipped" claim. BINDS comment removed; a test asserts no branch can reintroduce it.
  - epic: 4
    action: "B-3 (<=20 ms p95) explicitly covers TOOL-OUTPUT queries (prd.md:683), and Story 4.4's state check cannot meet it on this platform: one batched `git check-ignore` subprocess measures 47-55 ms (git status --porcelain 55.1, git ls-files 46.8) and hashing this repo's 171-file / 3.56 MB tracked tree measures 13.5 / 15.8 / 19.2 ms min/median/max, byte-dominated and growing with content. So the floor is ~60-75 ms before any Cortex logic. DELIBERATELY NOT PRE-EMPTED at create-story: narrowing the input set to fit a latency number is exactly how a false pass gets manufactured (SM-C3), and the B-4a precedent is to rule on measurements rather than estimates. Story 4.4 builds to the correctness ACs unconditionally, measures the real p95 through the real surface at default ceilings on this repo AND a seeded 2,000-file tree, reports met/not-met honestly, and brings candidate shapes here."
    owner: "ShuromiU (PRD decision)"
    status: done  # 2026-08-03 RULING (ShuromiU), on the measured numbers rather than the estimates: 65.9 / 68.7 / 80.0 ms is ACCEPTED for the command ledger. Rationale in his words: the figure has no negative bearing on the product - the query is user-initiated, runs once, is not on any hot path, and does not sit between the agent and its work - so a tighter number would be bravado rather than value. Explicitly conditioned on NOT trading accuracy for speed: the input set stays the full git-listed working tree. Cortex reported that no accuracy-preserving improvement is available for the dominant cost (one `git ls-files` subprocess, 31-80 ms, against a measured ~40 ms process-spawn floor on this platform); the only remaining lever is skipping the 13-19 ms hash on the MISS path when file count or total size already differ, worth ~20% on misses only, and it was NOT taken before review because it adds a code path for a modest gain. B-3 stands unchanged for the read and negative-result ledgers, which do meet it; the tool-output query is carved out at the measured figure.
